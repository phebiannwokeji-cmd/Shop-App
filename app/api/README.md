# app/api

Route handlers for the Next.js App Router live here.

Planned: a WhatsApp webhook that parses counter messages (sales, expenses,
debts) and calls `shopService` — the same shared brain the React UI uses.
Not yet wired; see README "Next Steps".